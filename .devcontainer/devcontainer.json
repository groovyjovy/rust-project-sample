{
    "name": "Rust",
    "dockerComposeFile": ["../docker-compose.yml"],
    "service": "api",
    "workspaceFolder": "/api",
    "customizations": {
        "vscode": {
            "extensions": [
                "rust-lang.rust-analyzer"
            ],
            "settings": {}
        }
    }
}
